class RecentUrlModel{
RecentUrlModel({
  required this.site,
  required this.url,
  required this.shortenedUrl,
  required this.time,

});

String site;
String url;
String shortenedUrl;
String time;
}